% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomarker_utils.R
\name{.GetCVAdjustContext}
\alias{.GetCVAdjustContext}
\title{Assemble the fold-wise covariate-adjustment context for CV, if active}
\usage{
.GetCVAdjustContext(dataSet)
}
\arguments{
\item{dataSet}{The dataset object}
}
\value{
list(base.data, cls, primary.design, covariate.design) or NULL
}
\description{
Returns NULL when no covariate adjustment was requested, in which
case cross-validation runs on \code{dataSet$data.norm.transposed} unchanged.
When adjustment is active it returns the UN-adjusted base data, aligned class
labels, and pre-built design matrices so the CV loop can re-fit the adjustment
inside every training fold instead of using the globally-adjusted data.
}
