% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_arrow.R
\name{shadow_save}
\alias{shadow_save}
\title{Shadow save: Saves both .qs (for R) and .arrow (for Java)}
\usage{
shadow_save(obj, file)
}
\arguments{
\item{obj}{The object to save (matrix, data.frame, or list of these)}

\item{file}{The .qs file path (Arrow path is auto-derived)}
}
\value{
The verified absolute path to the Arrow file, or NULL if not applicable
}
\description{
Primary function for saving data during migration. Maintains backward
compatibility with existing R code while enabling zero-copy Java access.
}
\details{
ALWAYS preserves rownames as the first column (row_names_id) in Arrow files.
}
\examples{
\dontrun{
# Basic usage
shadow_save(result_matrix, "results.qs")

# Java then reads from "results.arrow" via:
# DataTable table = UniversalArrowProxy.openIfExists(arrowPath);
}
}
