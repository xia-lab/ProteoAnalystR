% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomarker_utils.R
\name{.BuildCovariateDesign}
\alias{.BuildCovariateDesign}
\title{Build covariate/primary-condition design matrices for fold-wise adjustment}
\usage{
.BuildCovariateDesign(meta.info, primary.condition, adj.vec)
}
\arguments{
\item{meta.info}{Data frame of sample metadata (rows aligned to data rows)}

\item{primary.condition}{Name of the primary condition column (kept, not removed)}

\item{adj.vec}{Character vector of covariate/batch column names to remove}
}
\value{
list(primary.design, covariate.design) or NULL covariate.design if none
}
\description{
Construct the design matrices used to regress covariate/batch
effects out of biomarker data. Only covariate \emph{values} (e.g. age, sex, batch)
are used here; the outcome/expression is never touched, so this is safe to
build once from all samples and subset per fold without leaking held-out
information. The intercept is dropped from the covariate design because
removeBatchEffect-style adjustment only subtracts covariate contributions
while preserving the primary condition (kept in \code{primary.design}).
}
