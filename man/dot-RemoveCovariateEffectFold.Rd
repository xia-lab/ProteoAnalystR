% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomarker_utils.R
\name{.RemoveCovariateEffectFold}
\alias{.RemoveCovariateEffectFold}
\title{Remove covariate effects fold-wise (leakage-free)}
\usage{
.RemoveCovariateEffectFold(x.train, x.test, primary.train, cov.train, cov.test)
}
\arguments{
\item{x.train}{Training expression, samples x features}

\item{x.test}{Held-out expression, samples x features}

\item{primary.train}{Primary-condition design for training rows (kept)}

\item{cov.train}{Covariate design (no intercept) for training rows (removed)}

\item{cov.test}{Covariate design (no intercept) for held-out rows (removed)}
}
\value{
list(train, test) with covariate effects removed
}
\description{
Estimate covariate-removal coefficients from TRAINING samples only
(least-squares fit of expression on the primary condition + covariates), then
subtract the covariate contribution from both the training and the held-out
test samples. The held-out fold contributes only its covariate \emph{values} (used
to build \code{cov.test}); its expression and outcome are never used to fit the
adjustment, so the resulting cross-validation performance is not inflated by
information leakage. This is the fold-wise analogue of limma::removeBatchEffect.
}
